---
card_id: t_ac17626c
status: superseded
type: 
priority: 0
created_at: 2026-07-30T15:18:23+00:00
started_at: 2026-08-02T12:47:15+00:00
completed_at: 
last_sync_at: 2026-08-07T19:38:07.076531+00:00
last_kanban_state: superseded
result: None
auto_generated: true
audits:
  - kanban-drift-2026-08-04
---

# [URGENCIA MAXIMA][bug][sist] kryx check inexistente + Guard quebra nix flake check em background + UI Kanban dessincronizada

**Card:** `t_ac17626c` | **Status:** `superseded`

## Descrição

## PRIORIDADE: URGENCIA MAXIMA (0)

## Sintoma de superficie

Quando o agente (`aura-decision` em massa) tenta rodar `nix flake check --keep-going --impure` no fluxo normal do workspace, o shell interativo do Hermes dispara o **[Kryonix Guard]** e bloqueia o comando:

```
[Kryonix Guard] O comando 'nix' foi bloqueado.
Use o ecossistema kryx para gerenciar o sistema.
Exemplo: kryx switch  —  kryx update
```

Isso afeta QUALQUER agente que tente validar mudancas no repositorio de desenvolvimento
(`~/Proyectos/kryonix-dev/repos/kryonix*`) usando o `nix` do PATH do usuario.

Agentes que estao em sandbox/sudo poluidor rodam direto. Agentes no shell Hermes
ficam 100% cegos para a validacao.

## Achados reais (auditoria 2026-07-30 11:15)

### A. Path mismatch

```
/etc/profiles/per-user/rocha/bin/nix          (shim bloqueador, 159320a7...)
/run/current-system/sw/bin/nix               (binario real, nix 2.35.1)
```

O shim no `/etc/profiles/per-user/rocha/bin/nix` e um bash script que imprime
a mensagem do Guard e da `exit 1`. O binario real esta em
`/run/current-system/sw/bin/nix` (Nix 2.35.1) e responde normalmente.

### B. kryx-cli (Kryonix Unified CLI) NAO tem subcomando `check`

```
$ kryx --help | grep -i check
(vazio)
$ kryx check
error: unrecognized subcommand 'check'
```

Subcomandos existentes: `switch, factory-reset, system, doctor, identity, setup,
theme, update, status, shell, search, clean, build, run, develop, repl, fmt,
completion`. Nenhum equivalente a `nix flake check`.

### C. UI Kanban dessincronizada do SQLite

A UI mostra 24 cards `repeated_failures` em "tarefas precisam de atencao"
(mark `!!!` e `!!`), mas o SQLite real (`hermes kanban list`) so tem 3 blocked
(do Jenpex), 1 done, 67 scheduled, 13 todo, 1 triage. A UI esta renderizando
estado PRE-mass-schedule.

### D. Cartoes do bug kryx switch (t_03e3dfb6) ja resolveram

Estado: `triage` (correto, preserva ID sem autoridade). Run history:
```
#1 reclaimed    (manual, intencional)
#2 blocked      (decisao Estrategica B)
#3 scheduled    (movido em massa, esperado)
#4 blocked      (re-bloqueado para anexar nota tecnica)
```

Sao **fluxos legais**, nao falhas. O `repeated_failures` da UI e falso positivo
do contador que se accumula em qualquer `block` recorrente.

## Causa raiz

1. O **kryx-cli Guard** foi projetado para bloquear `nix` direto no shell
   interativo do usuario (Anti-Vibe-Coding). Porem ele quebra o **fluxo de
   agentes em background** que dependem de `nix flake check` para validacao
   estatica antes de commits.
2. Nao existe `kryx check` nem wrapper equivalente para val estatic
3. A solucao e simples mas precisa ser oficial (AGENTS.md nao cobre): **o
   agente deve chamar o nix direto via path absoluto** (`/run/current-system/sw/bin/nix`)
   quando o PATH do shell ativa o guard.

## Estrategia de correcao (proposta pelo Arquiteto)

### C1. Documentar o bypass oficial

Adicionar a `kryonix-dev/AGENTS.md` e a `kryonix-dev/agents/AGENTS.md`:

```
## Validacao estatica sem kryx

O `[Kryonix Guard]` em /etc/profiles/per-user/rocha/bin/nix bloqueia nix direto
em shell interativo. Para agentes em background, use:

  PATH="/run/current-system/sw/bin:/run/wrappers/bin:/usr/bin:/usr/local/bin" \
    /run/current-system/sw/bin/nix --extra-experimental-features 'nix-command flakes' \
    flake check --keep-going --impure

Valido em 2026-07-30 contra insipiron + glacier + inspiron-nina (all checks passed).
```

### C2. Adicionar `kryx check` real (work item separado)

Card futuro: criar `kryx check` que internamente delega para
`/run/current-system/sw/bin/nix flake check --keep-going --impure` ou para
`kryonix-builder.sh` se existir.

### C3. Forcar refresh do dashboard Kanban

O usuario precisa dar F5 na UI ou fazer `hermes kanban boards switch kryonix`
para forcar a UI re-render do estado real.

### C4. Re-validar e fechar o cartao do bug kryx switch (t_03e3dfb6)

Depois que C1 + C3 estiverem aplicados:
- Aplicar Estrategia (B) em `repos/kryonixos/hosts/inspiron/default.nix`
  (overwriteBackup = true, mudanca ja em workdir)
- `git add` + `git commit` + `git push` direto em main
- `kryx switch` (gatilho humano, NAO automatizar)
- Atualizar comentario em t_03e3dfb6 e mover para done

## Nao-objetivos

- NAO desativar o Kryonix Guard globalmente (e protecao contra vibe-coding)
- NAO criar symlink fora do /etc/profiles/per-user (polui PATH)
- NAO mexer em /etc/kryonixos direto (regra AGENTS.md)
- NAO automatizar kryx switch

## Criterio de aceitacao

1. Documento AGENTS.md com bypass oficial + exemplo validado
2. `nix flake check` funciona em background com PATH sem o guard
3. UI Kanban re-renderiza o estado real (F5 do usuario)
4. Estrategia (B) do bug kryx switch testada em PROD com sucesso

## Validacoes pendentes

- [ ] patch AGENTS.md proposto (cartao filho)
- [ ] teste do bypass em background process
- [ ] coordenacao com o cartao t_03e3dfb6 (ja diagnosticado)
- [ ] commit Strategy (B) em repos/kryonixos
- [ ] push + merge + kryx switch (gatilho humano)

## Riscos

- Documentar PATH override no AGENTS.md pode encorajar outros agentes
  a bypassar o guard intencionalmente. Mitigacao: explicitar que APENAS
  para `nix flake check` em background, NAO para `nix build` ou `nix develop`.
- Workaround path-absoluto quebra se Nix for reinstalado em path diferente.
  Mitigacao: documentar o `which nix` para descobrir o path real.

## Rollback

- Remover a secao do AGENTS.md (reverte o bypass documentado)
- `kryx check` que NAO existir e simples: nunca foi deployado

## Referencias

- Card t_03e3dfb6 (ja diagnosticado, Estrategica B validada)
- Commit c7c741d (fix original, revertido por 7a7be84)
- Work-in-progress em ~/Proyectos/kryonix-dev/repos/kryonixos/hosts/inspiron/default.nix
- ~/Proyectos/kryonix-dev/AGENTS.md (regra dos agentes)
- ~/Proyectos/kryonix-dev/repos/kryonixos/AGENTS.md (Fluxo Valid vs Build)

## Execução timestamps

- **Iniciado:** `2026-08-02T12:47:15+00:00`

## Current run

`run_id: 290`

## Workspace

`/home/rocha/.hermes/kanban/boards/kryonix/workspaces/t_ac17626c`

## Audits

This card is part of the following audit(s):

- `[kanban-drift-2026-08-04](../Audits/kanban-drift-2026-08-04/STATE.md)` — **Kanban × Vault drift audit** (2026-08-04)
  - 7 cards flagged for drift; 1 urgent (clobber-protection), 6 batch-reopen
## Eventos recentes

| Timestamp | Tipo | Payload |
|-----------|------|---------|
| `2026-08-07T03:37:46+00:00` | `split` | {"to": "superseded", "splits_to": ["t_6e19cd19", "t_fdf7f5df", "t_fcd2ec73"], "notes": "Split em 3 cards: t_6e19cd19, t_fdf7f5df, t_fcd2ec73. Veja kanban-drift-2026-08-04 audit."} |
| `2026-08-02T12:52:43+00:00` | `commented` | {"author": "default", "len": 869} |
| `2026-08-02T12:51:56+00:00` | `commented` | {"author": "default", "len": 1635} |
| `2026-08-02T12:51:00+00:00` | `commented` | {"author": "orchestrator", "len": 829} |
| `2026-08-02T12:50:06+00:00` | `commented` | {"author": "default", "len": 1023} |
| `2026-08-02T12:47:15+00:00` | `claimed` | {"lock": "inspiron:337934", "expires": 1785675735, "run_id": 290} |
| `2026-08-02T12:47:10+00:00` | `commented` | {"author": "default", "len": 755} |
| `2026-07-30T15:18:23+00:00` | `created` | {"assignee": "default", "status": "ready", "parents": [], "tenant": "kryonix", "workspace_kind": "scratch", "workspace_path": null, "branch_name": null, "project_id": null, "skills": null, "goal_mode"... |

## Execuções recentes

| Run ID | Status | Outcome | Início | Fim |
|--------|--------|---------|--------|-----|
| `290` | running | None | 2026-08-02T12:47:15+00:00 |  |

---

_Auto-gerado por `kanban-sync.py` em 2026-08-07T19:38:07.076535+00:00. Para parar de sobrescrever, adicione `<!-- manual-override -->` no topo. Para editar metadados, edite o card no Kanban (este arquivo é derivado)._