---
card_id: t_3b1197fa
status: ready
type: 
priority: 2
created_at: 2026-08-04T21:52:16+00:00
started_at: 
completed_at: 
last_sync_at: 2026-08-07T19:38:06.968654+00:00
last_kanban_state: ready
result: None
auto_generated: true
audits: []
---

# [kryxd][kryx][dead-code] KCR-TRANSLATOR-1: deletar translator.rs morto (455 LoC + 6 testes órfãos)

**Card:** `t_3b1197fa` | **Status:** `ready`

## Descrição

## Contexto

`crates/kryx/src/services/translator.rs` (455 LoC) emite diretivas Nix mortas. O tradutor foi escrito assumindo que o motor `kryonix` teria um módulo `kryonix.storage.*` consumindo o output — esse módulo nunca foi implementado (ou foi removido). O tradutor ficou órfão.

## Evidência

```rust
// crates/kryx/src/services/translator.rs:228
assert!(!result.contains("kryonix.thinkServer"));

// linhas 233-239 — testes explicitamente verificam que o tradutor NÃO emite
assert!(!result.contains("kryonix.storage.topology"));
assert!(!result.contains("kryonix.storage.systemDisks"));
assert!(!result.contains("kryonix.storage.dataDisks"));
assert!(!result.contains("kryonix.storage.root.filesystem"));
// ... etc
```

**Quem realmente faz o trabalho de gerar Nix:**

```bash
# O renderer REAL do disko está em src/services/partitioner.rs:
ls src/services/partitioner.rs
# 1109 linhas, com:
#   - validate_storage_contract (canonical StoragePlan validator)
#   - DiskoRenderer::render() (gera disko-config.nix)
#   - render_single / render_split / render_zpool
```

**O motor Nix declara:**

```bash
grep -rn 'options\.kryonix\.' ~/Proyectos/kryonix-dev/repos/kryonix/modules
# Apenas: kryonix.features.*, kryonix.branding, kryonix.kernel.cachyos,
# kryonix.security.*, kryonix.desktop.*, kryonix.shell.*
# NÃO declara: kryonix.storage.*, kryonix.thinkServer
```

**Cadeia de chamadas:**

```rust
// src/api/install.rs:359
let config_content = kryx::services::translator::generate_nix_config(&plan)
```

Ou seja: `generate_nix_config` é chamado mas seu output é silenciosamente ignorado pelo motor.

## Local

- `crates/kryx/src/services/translator.rs` (455 LoC, deletar)
- 6 testes órfãos (linhas 228-450 do mesmo arquivo, deletar junto)
- 1 caller: `src/api/install.rs:359` (atualizar pra remover a chamada)

## Fix esperado

**Opção A (recomendada):** Deletar `translator.rs` + 6 testes. Remover a chamada em `src/api/install.rs:359`. Se `generate_nix_config` for usado em outro lugar, atualizar o caller.

```bash
git rm crates/kryx/src/services/translator.rs
```

Migrar 2 dos asserts órfãos (que ainda protegem lógica legítima do translator histórico, se houver) pra tests do `DiskoRenderer` em `src/services/partitioner.rs::tests`.

## Validação

```bash
cd ~/Proyectos/kryonix-dev/repos/kryxd
grep -rn 'translator::generate_nix_config\|translator::' --include='*.rs'
# Esperado: 0 matches após remoção

cargo build --workspace
cargo test --workspace
```

## Bloqueado por

Nada. Build compila.

## Esforço

30-60min (1 arquivo deletado, 1 caller atualizado, 6 testes órfãos deletados).

## Refs

- audit completo: `~/Proyectos/kryonix-dev/repos/kryonix-vault/02-Areas/Kryonix/canonical/audits/2026-08-03-kryxd-daemon-structural-audit/10-structural-audit.md` (Achado #4)
- skill: `kryonix-architecture-audit` (pitfall 'código morto em fronteira Rust/Nix')
- ROADMAP: `_roadmap_kryxd-daemon-debt.md`

## External ID

`kcr-translator-1`

## Eventos recentes

| Timestamp | Tipo | Payload |
|-----------|------|---------|
| `2026-08-04T21:52:16+00:00` | `created` | {"assignee": "default", "status": "ready", "parents": [], "tenant": null, "workspace_kind": "scratch", "workspace_path": null, "branch_name": null, "project_id": null, "skills": null, "goal_mode": nul... |

---

_Auto-gerado por `kanban-sync.py` em 2026-08-07T19:38:06.968658+00:00. Para parar de sobrescrever, adicione `<!-- manual-override -->` no topo. Para editar metadados, edite o card no Kanban (este arquivo é derivado)._